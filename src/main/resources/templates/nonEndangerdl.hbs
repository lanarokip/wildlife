{{#partial "content"}}
    <nav class="navbar navbar-expand-lg navbar-light bg-dark">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link text-danger" href="/">Home <span class="sr-only">heroes</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-danger" href="#"> squad</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-danger" href="#">hero</a>
                </li>
            </ul>
        </div>
    </nav>


    {{#if not-endangerd}}
        {{#each not-endangerd}}
            <div class="card col-lg-9">
                <h1 class="text-center card-title" id="squad"> animal name:{{animal}}</h1>
            </div>
        {{/each}}
    {{else}}
        <div class="card col-md-12">

            <p id="hero" style="font-size:32px" class="card-text">there is no animal</p>
        </div>
    {{/if}}
{{/partial}}
{{> layout.hbs}}